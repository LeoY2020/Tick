<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Tick.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Tick.Services"
    Title="Tick">
    <NavigationView
        x:Name="NavView"
        PaneDisplayMode="Left"
        IsSettingsVisible="False"
        IsBackButtonVisible="Collapsed"
        SelectionChanged="OnNavSelectionChanged"
        OpenPaneLength="248">

        <NavigationView.PaneHeader>
            <StackPanel Padding="12,10,12,6" Spacing="6">
                <Button
                    x:Name="NewGoalButton"
                    Click="OnNewGoalClick"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Left"
                    Padding="10,6">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE710;" FontSize="14" />
                        <TextBlock Text="{local:L Key=goals.add}" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </NavigationView.PaneHeader>

        <Frame x:Name="ContentFrame" />
    </NavigationView>
</Window>